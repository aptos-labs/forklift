name: Check Lockfiles

on:
  pull_request:
  workflow_dispatch:

jobs:
  check-lockfiles:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Check forklift lockfile
        working-directory: packages/forklift
        run: |
          npm install --package-lock-only --ignore-scripts
          if ! git diff --exit-code package-lock.json; then
            echo "Error: packages/forklift/package-lock.json is not up to date."
            echo "Please run 'npm install' in packages/forklift and commit the changes."
            exit 1
          fi

      - name: Check testsuite lockfile
        working-directory: packages/testsuite
        run: |
          npm install --package-lock-only --ignore-scripts
          if ! git diff --exit-code package-lock.json; then
            echo "Error: packages/testsuite/package-lock.json is not up to date."
            echo "Please run 'npm install' in packages/testsuite and commit the changes."
            exit 1
          fi

      - name: Check example-tip-jar lockfile
        working-directory: packages/example-tip-jar
        run: |
          npm install --package-lock-only --ignore-scripts
          if ! git diff --exit-code package-lock.json; then
            echo "Error: packages/example-tip-jar/package-lock.json is not up to date."
            echo "Please run 'npm install' in packages/example-tip-jar and commit the changes."
            exit 1
          fi

      - name: Check live-tests lockfile
        working-directory: packages/live-tests
        run: |
          npm install --package-lock-only --ignore-scripts
          if ! git diff --exit-code package-lock.json; then
            echo "Error: packages/live-tests/package-lock.json is not up to date."
            echo "Please run 'npm install' in packages/live-tests and commit the changes."
            exit 1
          fi
